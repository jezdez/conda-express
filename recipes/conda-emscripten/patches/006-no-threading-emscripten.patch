diff --git a/conda/common/io.py b/conda/common/io.py
index fb34816..0b00d5d 100644
--- a/conda/common/io.py
+++ b/conda/common/io.py
@@ -478,7 +478,7 @@ def timeout(
 class DummyExecutor(Executor):
     """Synchronous executor for debugging; executes tasks in the current thread."""
 
-    def __init__(self) -> None:
+    def __init__(self, *args, **kwargs) -> None:
         self._shutdown = False
         self._shutdownLock = Lock()
 
@@ -512,7 +512,13 @@ class DummyExecutor(Executor):
             self._shutdown = True
 
 
-class ThreadLimitedThreadPoolExecutor(ThreadPoolExecutor):
+if sys.platform == "emscripten":
+    # Emscripten has no threading support; alias to DummyExecutor so all
+    # callers that pick ThreadLimitedThreadPoolExecutor get synchronous
+    # execution automatically.
+    ThreadLimitedThreadPoolExecutor = DummyExecutor
+else:
+  class ThreadLimitedThreadPoolExecutor(ThreadPoolExecutor):
     """Thread pool executor that gracefully handles thread creation limits."""
 
     def __init__(self, max_workers: int = 10) -> None:
diff --git a/conda/core/package_cache_data.py b/conda/core/package_cache_data.py
index c1c0f0d..f7d6890 100644
--- a/conda/core/package_cache_data.py
+++ b/conda/core/package_cache_data.py
@@ -6,7 +6,13 @@ from __future__ import annotations
 
 import os
 from collections import defaultdict
-from concurrent.futures import CancelledError, ThreadPoolExecutor, as_completed
+from concurrent.futures import CancelledError, as_completed
+import sys
+
+if sys.platform == "emscripten":
+    from ..common.io import DummyExecutor as ThreadPoolExecutor
+else:
+    from concurrent.futures import ThreadPoolExecutor
 from errno import EACCES, ENOENT, EPERM, EROFS
 from functools import partial
 from itertools import chain
diff --git a/conda/notices/fetch.py b/conda/notices/fetch.py
index d050661..0db1ff0 100644
--- a/conda/notices/fetch.py
+++ b/conda/notices/fetch.py
@@ -5,7 +5,12 @@
 from __future__ import annotations
 
 import logging
-from concurrent.futures import ThreadPoolExecutor
+import sys
+
+if sys.platform == "emscripten":
+    from ..common.io import DummyExecutor as ThreadPoolExecutor
+else:
+    from concurrent.futures import ThreadPoolExecutor
 from typing import TYPE_CHECKING
 
 import requests  # noqa: TID253
diff --git a/conda/plugins/reporter_backends/console.py b/conda/plugins/reporter_backends/console.py
index 18cc2a6..935929c 100644
--- a/conda/plugins/reporter_backends/console.py
+++ b/conda/plugins/reporter_backends/console.py
@@ -242,7 +242,7 @@ class ConsoleReporterRenderer(ReporterRendererBase):
         """
         Determines whether to return a TQDMProgressBar or QuietProgressBar
         """
-        if context.quiet:
+        if context.quiet or sys.platform == "emscripten":
             return QuietProgressBar(description, **kwargs)
         else:
             return TQDMProgressBar(description, **kwargs)
@@ -251,7 +251,7 @@ class ConsoleReporterRenderer(ReporterRendererBase):
         """
         Determines whether to return a Spinner or QuietSpinner
         """
-        if context.quiet:
+        if context.quiet or sys.platform == "emscripten":
             return QuietSpinner(message, fail_message)
         else:
             return Spinner(message, fail_message)
